---
title: CLI
description: xpengagent CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

xpengagent CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
xpengagent
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, xpengagent ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
xpengagent run "Explain how closures work in JavaScript"
```

---

### tui

xpengagent terminal kullanıcı arayüzünü başlatır.

```bash
xpengagent [project]
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                    |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                        |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                 |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Kullanılacak prompt                                                         |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak agent                                                          |
| <nobr><code>{"--port"}</code></nobr>     |      | Dinlenecek port                                                             |
| <nobr><code>{"--hostname"}</code></nobr> |      | Dinlenecek host adı                                                         |

---

## Komutlar

xpengagent CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

xpengagent için agent'ları yönetin.

```bash
xpengagent agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir xpengagent arka uç sunucusuna bir terminal ekleyin.

```bash
xpengagent attach [url]
```

Bu, TUI öğesinin uzak bir xpengagent arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
xpengagent web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
xpengagent attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak                                   | Kısa | Tanım                                                                                         |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | TUI'yi başlatmak için çalışma dizini                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum açma bilgileri                                                            |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu çatalla (`--continue` veya `--session` ile kullanın)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `XPENGAGENT_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `XPENGAGENT_SERVER_USERNAME` veya `xpengagent`) |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
xpengagent agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
xpengagent agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
xpengagent auth [command]
```

---

#### login

xpengagent, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `xpengagent auth login` kullanabilirsiniz. Bu `~/.local/share/xpengagent/auth.json`'de saklanıyor.

```bash
xpengagent auth login
```

xpengagent başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
xpengagent auth list
```

Veya kısa versiyonu.

```bash
xpengagent auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
xpengagent auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
xpengagent github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
xpengagent github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
xpengagent github run
```

##### Bayraklar

| Bayrak                                | Açıklama                                    |
| ------------------------------------- | ------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Aracıyı çalıştırmak için GitHub sahte olayı |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
xpengagent mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
xpengagent mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
xpengagent mcp list
```

Veya kısa versiyonunu kullanın.

```bash
xpengagent mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
xpengagent mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
xpengagent mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
xpengagent mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
xpengagent mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
xpengagent mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
xpengagent models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
xpengagent models anthropic
```

#### Bayraklar

| Bayrak                                  | Tanım                                                                       |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Modeller.dev'den model önbelleğini yenileyin                                |
| <nobr><code>{"--verbose"}</code></nobr> | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları xpengagent'da görmek istediğinizde kullanışlıdır.

```bash
xpengagent models --refresh
```

---

### run

Doğrudan bir istem ileterek xpengagent'u etkileşimli olmayan modda çalıştırın.

```bash
xpengagent run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "xpengagent run"
xpengagent run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `xpengagent serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
xpengagent serve

# In another terminal, run commands that attach to it
xpengagent run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                                      |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Çalıştırılacak komut, args için mesajı kullanın                                               |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                                   |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)                   |
| <nobr><code>{"--share"}</code></nobr>    |      | Oturumu paylaşın                                                                              |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak temsilci                                                                         |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Mesaja eklenecek dosya(lar)                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                            |
| <nobr><code>{"--title"}</code></nobr>    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)                      |
| <nobr><code>{"--attach"}</code></nobr>   |      | Çalışan bir xpengagent sunucusuna ekleyin (ör. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `XPENGAGENT_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `XPENGAGENT_SERVER_USERNAME` veya `xpengagent`) |
| <nobr><code>{"--dir"}</code></nobr>      |      | Çalıştırılacak dizin veya bağlanırken uzak sunucudaki yol                                     |
| <nobr><code>{"--variant"}</code></nobr>  |      | Model varyantı (sağlayıcıya özgü muhakeme düzeyi)                                             |
| <nobr><code>{"--thinking"}</code></nobr> |      | Düşünme bloklarını göster                                                                     |
| <nobr><code>{"--port"}</code></nobr>     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır)             |

---

### serve

API erişimi için headless bir xpengagent sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
xpengagent serve
```

Bu, TUI arayüzü olmadan xpengagent işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `XPENGAGENT_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `xpengagent` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

xpengagent oturumlarını yönetin.

```bash
xpengagent session [command]
```

---

#### list

Tüm xpengagent oturumlarını listeleyin.

```bash
xpengagent session list
```

##### Bayraklar

| Bayrak                                    | Kısa | Tanım                                  |
| ----------------------------------------- | ---- | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | En son N oturumla sınırla              |
| <nobr><code>{"--format"}</code></nobr>    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

xpengagent oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
xpengagent stats
```

#### Bayraklar

| Bayrak                                  | Açıklama                                                                                                    |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Gösterilecek araç sayısı (tümü)                                                                             |
| <nobr><code>{"--models"}</code></nobr>  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| <nobr><code>{"--project"}</code></nobr> | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
xpengagent export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya xpengagent paylaşımından URL oturum verilerini içe aktarın.

```bash
xpengagent import <file>
```

Yerel bir dosyadan veya xpengagent paylaşımından (URL) içe aktarabilirsiniz.

```bash
xpengagent import session.json
xpengagent import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir xpengagent sunucusu başlatın.

```bash
xpengagent web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla xpengagent'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `XPENGAGENT_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `xpengagent` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
xpengagent acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak                                   | Açıklama            |
| ---------------------------------------- | ------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Çalışma dizini      |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek port     |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek host adı |

---

### uninstall

xpengagent'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
xpengagent uninstall
```

#### Bayraklar

| Bayrak                                      | Kısa | Tanım                                           |
| ------------------------------------------- | ---- | ----------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Yapılandırma dosyalarını sakla                  |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Onay istemlerini atla                           |

---

### upgrade

xpengagent'u en son sürüme veya belirli bir sürüme günceller.

```bash
xpengagent upgrade [target]
```

En son sürüme yükseltmek için.

```bash
xpengagent upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
xpengagent upgrade v0.1.48
```

#### Bayraklar

| Bayrak                                 | Kısa | Açıklama                                               |
| -------------------------------------- | ---- | ------------------------------------------------------ |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

xpengagent CLI aşağıdaki global bayrakları destekler.

| Bayrak                                     | Kısa | Tanım                                    |
| ------------------------------------------ | ---- | ---------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Yardımı görüntüle                        |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Sürüm numarasını yazdır                  |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Günlükleri stderr'e yazdır               |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

xpengagent ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `XPENGAGENT_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `XPENGAGENT_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `XPENGAGENT_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `XPENGAGENT_TUI_CONFIG`                 | string  | TUI yapılandırma dosyasının yolu                                            |
| `XPENGAGENT_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `XPENGAGENT_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `XPENGAGENT_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `XPENGAGENT_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `XPENGAGENT_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `XPENGAGENT_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `XPENGAGENT_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `XPENGAGENT_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `XPENGAGENT_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `XPENGAGENT_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `XPENGAGENT_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `XPENGAGENT_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `XPENGAGENT_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `XPENGAGENT_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `XPENGAGENT_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `XPENGAGENT_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `XPENGAGENT_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `XPENGAGENT_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `XPENGAGENT_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `xpengagent`) |
| `XPENGAGENT_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `XPENGAGENT_EXPERIMENTAL`                         | boolean | Şemsiye bayrağa bağlı deneysel özellikleri etkinleştir  |
| `XPENGAGENT_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `XPENGAGENT_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `XPENGAGENT_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `XPENGAGENT_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `XPENGAGENT_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `XPENGAGENT_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `XPENGAGENT_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `XPENGAGENT_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `XPENGAGENT_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `XPENGAGENT_EXPERIMENTAL_LSP_TY`                  | boolean | python dosyaları için TY LSP'yi etkinleştir             |
| `XPENGAGENT_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
| `XPENGAGENT_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Arka plan alt ajan görevlerini etkinleştir              |
| `XPENGAGENT_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Deneysel olay sistemini etkinleştir                     |
| `XPENGAGENT_EXPERIMENTAL_NATIVE_LLM`              | boolean | Yerel LLM istek yolunu etkinleştir                      |
| `XPENGAGENT_EXPERIMENTAL_PARALLEL`                | boolean | Paralel web araması yürütmesini etkinleştir             |
| `XPENGAGENT_EXPERIMENTAL_SCOUT`                   | boolean | Scout alt ajanını etkinleştir                           |
| `XPENGAGENT_EXPERIMENTAL_WORKSPACES`              | boolean | Çalışma alanı desteğini etkinleştir                     |
