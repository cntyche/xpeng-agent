# v0.1.2 Release Notes

**Date**: 2026-06-28

## Bug Fixes

### P0 — XPENG Free 提供商模型配置错误
- 模型列表从多个硬编码模型 (`deepseek-chat`, `qwen-turbo` 等) 改为单一 `auto` 模型
- 由 API 端自动路由到可用模型，与 API key 实际权限匹配

### P0 — 启动崩溃 `Missing key at ["cost"][0]["cache"]`
- `xpengagent-free.ts`, `huocheng-websecurity.ts`, `provider.ts` 中所有 cost 对象补全 `cache` 字段
- 符合 `ProviderCost` Schema 校验要求

### P1 — `xpengagent upgrade` 升级失败
- GitHub 仓库引用从 `anomalyco/xpengagent` 全量迁移到 `cntyche/xpeng-agent`
- 涉及 30+ 文件（installation, console, desktop, containers, CI workflows, scripts 等）

### P1 — CLI 普通输出 Logo 仍显示 "opencode"
- `ui.ts` fallback wordmark 替换为 XPENGAGENT ASCII 块字符
- `presentation.ts` session 结束 Logo 同步更新

## Full Changelog

Full changelog available at https://github.com/cntyche/xpeng-agent/releases/tag/v0.1.2